#ifndef TOKEN_H
#define TOKEN_H

enum TokenType {
    // single-character tokens
    LEFT_PAREN, RIGHT_PAREN, LEFT_BRACE, RIGHT_BRACE, COMMA, DOT,
    MINUS, PLUS, SEMICOLOR, SLASH, STAR,

    // one or two character tokens

};
class Token {
};

#endif

